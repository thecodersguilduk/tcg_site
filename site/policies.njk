---
layout: default
meta_title: Policies and Procedures
meta_description: See All Our Policies And Procedures. We Use These Policies To Create A Safe, Happy Environment Where Wellbeing Is An Essential Component Of Our Business Offer.
body_class: policies
title: Policies - full list
---

<div class="container max-w-full sm:pt-0">
  <main id="main" class="main inner flex flex-1 flex-col pt-20 focus:outline-none md:mb-20 mb-12" tabindex="-1">
    <div class="pt-12 md:pt-14 md:px-16 sm:pt-10 max-w-3xl mx-auto">
      <div class="mb-10">
        <h1 class="md:text-5xl md:leading-5xl text-4xl leading-4xl">Policies</h1>
        <p>We take our policies very seriously. These are not just documents for compliance sake but they are an essential part of our organisational management and are our roadmap for successful day-to-day operations. We use these policies to create a safe, happy environment where wellbeing is an essential component of our business offer. We review these policies and procedures on a regular basis to ensure they meet the changing needs of the services that we provide and meet the needs of our staff and stakeholders.</p>
        <form>
          <label for="search" class="sr-only">Search a policy</label>
          <input type="text" name="search" id="search" class="mt-8 form-input-field rounded block w-full py-2 px-3 border-1 placeholder-black" placeholder="Search a policy" />
        </form>
      </div>
      <div class="policy-list">
        {% for policy in policies_list %}
          <a href="{{ policy.pdf_link }}" target="_blank" class="policy flex flex-wrap pointer hover:text-blue-200 py-4 px-6 border-1 border-grey-200 mb-4 md:mb-2 flex-test hover:shadow-primary focus:shadow-primary policy-item">
            <p class="font-bold sm:flex-2 w-full pr-4">{{ policy.title }}</p>
            <p class="text-sm flex-1 text-grey-300">{{ policy.version }}</p>
            <p class="text-sm flex-1 text-grey-300">{{ policy.updated_date | dateDisplay }}</p>
          </a>
        {% endfor %}
      </div>
    </div>
  </main>
</div>

<script>
const searchBar = document.getElementById('search')
const policyList = document.querySelector('policy-list')
const policies = Array.from(document.querySelectorAll('.policy'))
let searchTerm = ''

searchBar.addEventListener('keyup', (e) => {
  e.preventDefault();
  const value = e.target.value.toLowerCase()

  policies.forEach(policy => {
  if (value === "") {policy.classList.remove('hidden')}

    if(!policy.children[0].innerText.toLowerCase().includes(value)){
      policy.classList.add('hidden')
    } else{
      policy.classList.remove('hidden')
    }
  })
})
</script>