---
layout: default
related_articles: true
---

{# a post layout #}
<main id="main" class="main inner flex flex-1 flex-col py-10 lg:py-20 focus:outline-none" tabindex="-1">

    <article id="container-centre" class="column centre flex-1 md:mx-20 mb-24">

        <div class="mb-10">
            {# featured post image #}
            {% if featured_image %}

                <figure class="">
                    <img data-lazy="{{ featured_image }}" alt="{{ image_caption }}">
                </figure>

            {% endif %}

            <div class="md:px-20 relative pt-10">
              {% if tags %}
                <div class="category absolute top-0 flex -my-4 -mx-1">
                  {% for item in tags %}
                    {%- if item != "blog" -%}
                      <div class="px-1">
                        <a href="/category/{{ item }}" class="px-2 py-1 bg-red-100 text-white font-semibold rounded block">{{ item | capitalize }}</a>
                      </div>
                    {%- endif -%}
                  {% endfor %}
                </div>
              {% endif %}

              <header>
                  <h1 class="text-blue-100 max-w-90">{{ title }}</h1>
              </header>

              <div class="flex">

                <div class="mr-4">
                  <img src="../../images/blog/{{avatar}}" class="h-12 w-12 object-cover rounded-50 block" />
                </div>

                <div>
                  <p class="time text-blue-200 font-semibold">{{ time }} read time</p>
                  <div class="flex">
                    <p class="author text-gray-200">By {{ author }}</p>
                    <p> - </p>
                    <time datetime="{{ date }}" class="text-gray-200">
                        {{ date | dateDisplay }}
                    </time>
                  </div>
                </div>
              </div>
            </div>

        </div>

        <div class="content post md:px-20">

            {{ content | safe }}

        </div>

        <div class="mt-8 md:px-20">

            <div class="border-t border-gray-200 pt-6">

            {% if tags %}
              <div>
                <ul class="flex">
                  {% for item in tags %}
                    {%- if item != "blog" -%}
                      <li class="mr-2">
                          <a class="px-2 py-1 bg-red-100 text-white font-semibold rounded block" href="/category/{{ item }}">{{ item | capitalize }}</a>
                      </li>
                    {%- endif -%}
                  {% endfor %}
                </ul>
              </div>

            {% endif %}
              {% if prevPost or nextPost %}
                <div class="mt-6">
                  {% if prevPost %}<p class="text-gray-200"><span class="font-heading text-gray-200 text-sm font-bold">Previous:</span> <a href="{{ prevPost.url }}" class="text-gray-200 font-heading text-sm font-bold link">{{ prevPost.data.title }}</a></p>{% endif %}
                  {% if nextPost %}<p class="text-gray-200 font-heading"><span class="font-semibold text-sm font-bold">Next:</span> <a href="{{ nextPost.url }}" class="border-b-2 border-blue-100 text-gray-200 font-heading text-sm font-bold link">{{ nextPost.data.title }}</a></p>{% endif %}
                </div>
              {% endif %}

        </div>

    </article>

    {% include "components/blog/blog-section.njk" %}

</main>
