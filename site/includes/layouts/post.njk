---
layout: default
related_articles: true
---

{# a post layout #}
<main id="main" class="main flex flex-1 flex-col focus:outline-none" tabindex="-1">

    <article id="container-centre" class="column centre flex-1 mb-20 mx-auto pt-20">

        <div class="mb-8 max-w-5xl">
            {% if featured_image %}

                <figure class="">
                    <img data-lazy="{{ featured_image }}" alt="{{ image_caption }}" class="max-h-68 max-w-4xl object-cover">
                </figure>

            {% endif %}

            <div class="md:px-20 relative pt-10 max-w-3xl mx-auto inner">
              {% if tags %}
                <div class="category absolute top-0 flex -my-4 -mx-1">
                  {% for item in tags %}
                    {%- if item != "blog" -%}
                      <div class="px-1">
                        <a href="/category/{{ item }}" class="p-2 bg-red-100 text-white font-bold rounded block font-heading text-default leading-sm">{{ item | capitalize }}</a>
                      </div>
                    {%- endif -%}
                  {% endfor %}
                </div>
              {% endif %}

              <header>
                  <h1 class="text-blue-200">{{ title }}</h1>
              </header>

              <div class="flex">

                <div class="mr-2 w-10">
                  <img src="../../images/blog/{{avatar}}" class="w-full object-cover rounded-50 block" />
                </div>

                <div class="my-auto">
                  <p class="time text-blue-200 font-bold font-heading mb-1 text-default leading-sm">{{ time }} read time</p>
                  <div class="">
                    <p class="author text-black text-0 leading-xs">
                      By {{ author }}
                      <span class="text-black text-0 leading-xs my-auto">&#8210;</span>
                      <time datetime="{{ date }}" class="text-black text-0 my-auto">
                        {{ date | dateDisplay }}
                      </time>
                    </p>
                  </div>
                </div>
              </div>
            </div>

        </div>

        <div class="content post md:px-20 max-w-3xl mx-auto inner">

            {{ content | safe }}

        </div>

        <div class="mt-10 md:px-20 max-w-3xl mx-auto inner">

            <div class="border-t border-grey-300 pt-8">

            {% if tags %}
              <div>
                <ul class="flex">
                  {% for item in tags %}
                    {%- if item != "blog" -%}
                      <li class="mr-2">
                          <a class="p-2 bg-red-100 text-white font-bold rounded block font-heading text-default leading-sm" href="/category/{{ item }}">{{ item | capitalize }}</a>
                      </li>
                    {%- endif -%}
                  {% endfor %}
                </ul>
              </div>

            {% endif %}
              {% if prevPost or nextPost %}
                <div class="mt-6">
                  {% if nextPost %}
                    <div {% if prevPost %}class="mb-4"{% endif %}>
                      <p class="text-black"><span class="font-heading text-black text-base font-bold">Next article:</span> <a href="{{ nextPost.url }}" class="text-black font-heading text-base font-bold border-b-2 border-blue-100 pb-1">{{ nextPost.data.title }}</a></p>
                    </div>
                  {% endif %}
                  {% if prevPost %}
                    <div>
                      <p class="text-black"><span class="font-heading text-black text-base font-bold">Previous article:</span> <a href="{{ prevPost.url }}" class="text-black font-heading text-base font-bold border-b-2 border-blue-100 pb-1">{{ prevPost.data.title }}</a></p>
                    </div>
                  {% endif %}
                </div>
              {% endif %}

        </div>

    </article>

    {% include "components/blog/blog-section.njk" %}

</main>
