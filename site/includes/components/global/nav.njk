<div class="nav overflow-x-hidden">
  <div class="md:hidden">
    <button id="nav-toggle" class="burger relative block z-20">
      <span class="block bg-black"></span>
      <span class="block bg-black"></span>
      <span class="block bg-black"></span>
    </button>

    <div class="mobile-nav-container opacity-0 fixed z-10 h-full flex items-center justify-center bg-white">
      <nav id="mobile-nav">
        <ul class="">
        {% for nav_link in navigation.main %}
          <li class="my-6 text-center">
            <a href="{{ nav_link.url | safe }}" class="block py-3 font-semibold capitalize">{{ nav_link.label | safe }}
            </a>
          </li>
        {% endfor %}
          <li class="my-6 text-center">
            <a href="" class="block py-3 font-semibold rounded">Book a Call</a>
          </li>
        </ul>
      </nav>
    </div>

  </div>


  <nav id="nav-links" class="nav-links hidden md:block">
    <div class="nav-links-inner">
      <ul class="flex -mx-3">
      {% for nav_link in navigation.main %}
        <li class="px-3">
          <a href="{{ nav_link.url | safe }}" class="block py-2 px-3 font-semibold text-gray-200 font-heading hover:text-blue-200 focus:text-blue-100 active:text-blue-100 capitalize">{{ nav_link.label | safe }}
          </a>
        </li>
      {% endfor %}
        <li class="px-3">
          <a href="" class="block py-2 px-6 font-semibold bg-blue-100 text-white rounded font-heading hover:bg-blue-200">Book a Call</a>
        </li>
      </ul>
    </div>
  </nav>
</div>
        
    
